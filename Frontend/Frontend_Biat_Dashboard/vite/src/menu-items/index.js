import dashboard, { system } from './dashboard';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [dashboard, system]
};

export default menuItems;
